//now let's add the color that we will need to project
import 'package:flutter/material.dart';

class MainColor {
  static Color primaryColor = const Color.fromARGB(255, 63, 223, 130);
  static Color secondaryColor = const Color.fromARGB(255, 14, 251, 6);
  static Color accentColor = const Color.fromARGB(255, 238, 185, 12);
}
